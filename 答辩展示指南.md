# 聆心平台答辩展示指南

## 演示前准备

1. 确认依赖已安装，`.env` 中已配置可用的 `DASHSCOPE_API_KEY`。
2. 运行一键脚本生成或校验演示数据：

```bash
python scripts/verify_test_accounts.py
```

如需重新生成确定性数据：

```bash
python seed_v31.py
```

3. 确认测试通过：

```bash
python -m pytest tests/ -q
python scripts/smoke_frontend.py
```

4. 提前准备两个浏览器窗口：
   - 主窗口：辅导员 `zhangwei / counsel123`；
   - 备用窗口：学生 `20240001 / 123456`，或登录页动态展示的学生账号。

## 8-10 分钟演示脚本

| 时间 | 环节 | 操作与讲解 |
|---|---|---|
| 0:00-0:30 | 开场 | 一句话定位：面向高校辅导员的 AI 减负与心理预警一体化平台。 |
| 0:30-1:20 | 登录与工作台 | 展示管理员/辅导员/学生三角色入口；强调账号由接口动态下发，每名辅导员固定 50 名学生，保证网络图美观。 |
| 1:20-2:30 | 情绪网络图 | 打开情绪网络图，展示风险颜色、班级/学院聚类、筛选、搜索、节点点击直达学生聊天。 |
| 2:30-4:00 | 视频实时情绪 | 发起视频通话，展示微表情 + 语音韵律 + 后端模型融合；结束通话后形成情绪总结并回写学生风险。 |
| 4:00-5:30 | AI 数字人 | 学生端向离线老师提问，展示 AI 分身回复、AI 身份标注、TTS 朗读、颜文字/表情包话术。 |
| 5:30-7:00 | 危机闭环 | 学生输入“我最近有不想活的想法”，数字人触发危机预警；学工处进入危机工单中心，完成查看、指派、关闭。 |
| 7:00-8:20 | 学生状态证据链 | 打开学生详情，展示风险时间线：测评 high -> 谈心 medium -> 视频 high；讲解“取最高风险不降级”的安全保守策略。 |
| 8:20-9:00 | 测试与工程 | 快速切换到命令行展示 `pytest` 全绿、前端冒烟、账号校验；说明 XSS 防护和关键 API 错误提示。 |
| 9:00-9:30 | 收束 | 总结：功能闭环 + 工程可交付 + 心理安全合规。 |

## 亮点话术

### 1. 多模态情绪融合

“我们把面部表情、语音韵律和语义模型放在同一时间轴融合，而不是只靠单一模态。这样识别更稳定，误报更少。”

### 2. 状态聚合取最高

“测评、谈心、视频、数字人、预警处置都会回写学生状态，但风险只会向更高级别聚合，不会因为后续一次低风险记录就把前面的危机信号冲掉。”

### 3. AI 数字人

“老师不在线时，数字人以心理专家视角接住学生。它有 AI 身份标注，不会冒充真人；识别到危机时不是只写日志，而是进入预警和危机工单。”

### 4. 危机工单闭环

“危机是跨角色事件，所以我们提供了学工处视角的危机工单中心，支持查看、指派、关闭，并在关闭后重新计算学生风险。”

### 5. 工程可信

“项目不是页面堆砌，核心链路有自动化测试。当前测试覆盖闭环、权限、边界和前端冒烟，能防止改代码后悄悄白屏。”

## 应急备份

若某个模型接口暂时不可用：

- 视频情绪会自动降级到本地声学特征 + 前端表情融合；
- 数字人问答会给出可理解的降级回复；
- 语音情绪识别在模型缺失时使用规则和声学特征兜底（即使 API 与本地模型均不可用，接口也会返回标注 `_source=rule` 的保守判定，不会报错）；
- 现场优先展示已闭环的预警处置、状态时间线和网络图，避免依赖外部 API。

## 追问应答预案（评委深挖时的标准回答）

| 可能的追问 | 建议回答 |
|---|---|
| LangGraph 风险决策图在线上哪条链路里跑？ | 「数字人代班工作流是 LangGraph 生产链路（学生发消息→离线触发→条件分支：危机→预警/普通→回复）。风险决策图是同一套编排模式的验证实现，生产行为由数据库聚合层保证（取最高不降级），两者行为一致且有测试锁定；把它接入生产路径是下一步重构计划。」 |
| 视频通话多人同时打会不会串？ | 「当前为演示取舍：控制台临时 Token 绑定固定频道。后端已实现 App Certificate 动态签发（AccessToken007），上线切换配置即可支持动态频道与并发通话。」 |
| 实时情绪是模型还是规则？ | 「三路融合：语音走 emotion2vec 真模型、人脸走 FER 真模型、微表情/韵律走前端启发式。每个结果都带 `_source` 来源标注，我们刻意不伪装降级结果——这也是心理产品的诚实原则。」 |
| 为什么学生密码这么简单、登录页还展示账号？ | 「演示便利。`/system/test-accounts` 方便评委快速体验三角色；生产环境直接移除该接口并强制密码策略。」 |
| 预警产生后老师不在线怎么办？ | 「当前版本老师登录后工作台与预警中心立即可见，未读徽标会提示；短信/邮件推送通道在路线图下一步。数字人的价值正在于此：即使老师不在线，危机信号已被接住、留证、进工单，不会丢失。」 |
| 数据是真实的吗？ | 「种子数据是按真实辅导员工作场景构造的确定性演示数据（600 名学生、20 类真实谈话场景），并有 61 项数据一致性校验脚本。真实部署时对接学校统一身份与学工系统即可。」 |
